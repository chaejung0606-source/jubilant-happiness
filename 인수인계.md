# 인수인계 문서

다른 사람(또는 다른 계정)이 이 프로젝트를 이어받아 작업할 때 필요한 내용을 정리했습니다.
**이 문서만 읽으면 바로 이어서 작업할 수 있도록** 결정의 배경과 시행착오까지 적었습니다.

---

## 1. 이 프로그램이 하는 일

강원대학교 **데이터보안·활용 융합 혁신융합대학사업단**의 회의비 정산용 도구입니다.

회의 자료(공문·계획서·영수증·참석 서명부)가 든 **폴더 1개 = 회의 1건**으로 보고,
폴더마다 회의록 1건을 **한글 문서(`.hwpx`)** 로 만들어 **그 폴더 안에** 저장합니다.
사용자는 한글에서 열어 보완 내용을 고친 뒤 PDF로 변환해 정산 시스템에 올립니다.

회의 자료를 읽고 회의록 문안을 쓰는 일은 **사용자 PC 에 설치된 Claude Code CLI**가 합니다.
API 키를 쓰지 않고, 사용자의 Claude 구독으로 동작합니다.

---

## 2. 저장소·배포 정보

| 항목 | 값 |
|---|---|
| 저장소 | `chaejung0606-source/jubilant-happiness` (**공개**) |
| 작업 브랜치 | `claude/what-were-you-doing-repo-endi9l` |
| 릴리스 태그 | `app-latest` (매 빌드마다 삭제 후 재생성) |
| 릴리스 자산 | `MeetingMinutes.exe`, `core.py`, `version.json` |

> 저장소 이름과 설명("배구 심판 공부")은 이 프로젝트와 무관합니다. GitHub 자동 생성 이름을 그대로 씁니다.

---

## 3. 구조 — 가장 먼저 이해할 것

**`.exe` 는 껍데기이고, 기능은 `core.py` 에 있습니다.**

```
회의록도구.exe (실행기)          ← 거의 안 바뀜. 사용자는 한 번만 받음
   │
   │ 켤 때마다 릴리스에서 core.py 를 받아 sha256 검증 후 실행
   ↓
core.py (기능 본체)              ← 여기만 고치면 사용자에게 자동 반영
```

- 내려받은 `core.py` 는 `%LOCALAPPDATA%\MeetingMinutesTool\` 에 캐시
- 3단 안전장치: **최신본 → 캐시본 → `.exe` 내장본**
- 실행기가 `APP_DIR` 을 본체에 넘겨 `규칙.txt`·`설정.txt`·`오류기록.txt` 가 `.exe` 옆에 생기게 함

### 이렇게 만든 이유

사용자가 **"업데이트마다 `.exe` 가 교체되는 게 불편하다"** 고 요청했습니다.
이전에는 `.exe` 자체를 교체했는데, 파일이 바뀌고 확인 창이 뜨는 것을 싫어했습니다.
지금 구조에서는 사용자가 아무것도 하지 않아도 기능만 조용히 갱신됩니다.

### 작업 방식 (중요)

| 무엇을 고쳤나 | 사용자가 할 일 |
|---|---|
| **`core.py`** | 없음. 다음에 켜면 자동 반영 |
| **`회의록도구.pyw`** (실행기) | `.exe` 를 새로 받아야 함 → `LAUNCHER_VERSION` 을 올릴 것 |

**대부분의 작업은 `core.py` 만 고치면 됩니다.** 실행기는 웬만하면 건드리지 마세요.

실행기를 고쳐야 한다면, `core.py` 가 쓰는 모듈을 실행기 상단에서 `import` 해야 합니다.
본체는 실행 중에 내려받으므로 **PyInstaller 가 본체의 import 를 분석하지 못합니다.**

---

## 4. 배포 방법

```bash
# core.py 수정 → 커밋 → 푸시하면 끝
git add -A && git commit -m "..." && git push -u origin claude/what-were-you-doing-repo-endi9l
```

GitHub Actions(`.github/workflows/build-windows-app.yml`)가 windows-latest 에서 자동으로:
1. `.exe` 빌드
2. 실행 여부 확인 (화면이 없어 실패할 수 있어 `continue-on-error`)
3. `MeetingMinutes.exe`, `core.py`, `version.json` 을 릴리스에 업로드

빌드 완료 확인:
```bash
curl -sS "https://api.github.com/repos/chaejung0606-source/jubilant-happiness/releases/tags/app-latest"
```

### 배포 검증 방법

실제로 사용자가 켰을 때와 같은 경로를 재현할 수 있습니다.

```python
# 실행기를 import 해서 update_core() 를 부르면 릴리스에서 core.py 를 받아온다
path, fresh = L.update_core()
scope = {"__name__":"본체", "__file__":path, "__builtins__":__builtins__, "APP_DIR":L.app_dir()}
exec(compile(L._read_text(path), path, "exec"), scope)
# 이제 scope["process_meeting"] 등을 직접 호출해 확인
```

---

## 5. 사용자 환경 (실제 확인된 것)

| 항목 | 값 |
|---|---|
| OS | Windows 11, 사용자명 `User` |
| Claude 플랜 | **Pro** (Max 아님 — 한도가 빨리 참) |
| `claude.exe` 위치 | `C:\Users\User\.local\bin\claude.exe` |
| **PATH 등록** | **안 되어 있음** — cmd 에서 `claude` 만 치면 실패 |
| 한글(HWP) | 설치됨. `.hwpx` 정상 개봉 확인 |
| 회의 자료 위치 | `C:\Users\User\Desktop\★★지출진행\...` |
| 특이사항 | 임시 폴더 정리 프로그램(ESTsoft 계열) 가 `_MEI*` 를 지우는 것으로 보임 |

---

## 6. 반드시 알아야 할 함정들 (전부 실제로 겪은 것)

### 6-1. `.bat` 파일은 CRLF + CP949 여야 함
LF 줄바꿈이면 cmd 가 `if (...)` 블록을 잘못 파싱해 실행조차 안 됩니다.
UTF-8 + `chcp 65001` 조합도 코드페이지 전환 때 깨집니다.
`.gitattributes` 에 `*.bat -text` 로 git 이 건드리지 못하게 해두었습니다.

### 6-2. 빌드 서버 콘솔은 cp1252
`print("한글")` 만으로 `UnicodeEncodeError` 가 나며 빌드가 즉시 실패합니다.
`build_app.py` 의 `_fix_console()` 이 **한글을 못 찍는 콘솔에서만** UTF-8 로 전환합니다.
한국어 윈도우(cp949)는 그대로 둬야 로컬 실행 시 한글이 정상 표시됩니다.

### 6-3. GitHub 릴리스 첨부파일 이름에서 한글이 제거됨
`회의록도구.exe` 를 올리면 **`default.exe`** 가 됩니다.
그래서 `MeetingMinutes.exe` 라는 ASCII 이름으로 올리고 표시 이름만 한글로 답니다.
(Actions 아티팩트는 한글 이름이 유지됩니다. 릴리스만 다릅니다.)

### 6-4. PyInstaller onefile 의 `_MEI` 임시 폴더가 실행 중 사라질 수 있음
백신·정리 프로그램이 지웁니다. 그러면 **그때까지 쓰이지 않아 아직 import 되지 않은 코덱**을
불러오다 `FileNotFoundError: ...base_library.zip` 이 납니다.

이 때문에 `utf-8-sig` 로 파일을 읽던 코드가 실패해, Claude 가 정상 저장한 `meeting.json` 을
못 읽고 "회의록 데이터 없음" 으로 버리는 **최악의 버그**가 있었습니다.

**대응:**
- `_read_text()` 로 바이트를 읽고 BOM 을 직접 제거한 뒤 `utf-8` 로 디코딩 (utf-8 은 시작 시 이미 로드됨)
- 실행기 상단에서 `encodings.utf_8_sig`, `cp949`, `idna` 를 미리 import

**새 코드에서 `encoding="utf-8-sig"` 를 쓰지 마세요.** `_read_text()` 를 쓰세요.

### 6-5. `.hwpx` 표 병합은 `.docx` 와 방식이 다름
- `.docx`: 병합할 셀을 **모두 쓰고** `vMerge` 로 표시
- `.hwpx`: 첫 셀에 `rowSpan` 을 주고 **이후 행에서는 셀을 아예 생략**

`cellAddr`(colAddr/rowAddr)과 `cellSpan` 이 정확해야 하며, 격자에 **겹침이나 빈칸이 있으면
한글이 파일을 열지 못합니다.** 표 구조를 고칠 때는 반드시 격자 검사를 하세요.

```python
# 격자 검증 예시 — 겹침/빈칸이 없는지 확인
grid = {}
for tr in rows:
    for tc in tr.getElementsByTagName("hp:tc"):
        c, r = colAddr, rowAddr
        for dr in range(rowSpan):
            for dc in range(colSpan):
                assert (r+dr, c+dc) not in grid    # 겹침
                grid[(r+dr, c+dc)] = 1
assert all((r,c) in grid for r in range(R) for c in range(C))   # 빈칸
```

### 6-6. `.hwpx` 패키지 규칙
- `mimetype` 은 **첫 항목**이고 **무압축(STORED)** 이어야 함
- `header.xml` 에 정의한 id 를 `section0.xml` 이 참조 — **id 참조가 하나라도 깨지면 안 열림**
- `itemCnt` 선언값과 실제 개수가 일치해야 함

### 6-7. `--allowedTools` 에 `PowerShell` 이 필요
윈도우의 Claude Code 에는 `Bash` 와 별개로 `PowerShell` 도구가 있습니다.
허용하지 않으면 Claude 가 계속 거부당하며 **회의 1건에 22~33턴**을 낭비합니다.
`.pdf` 확장자인데 실제로는 한디소프트 전자결재 파일인 자료를 읽을 때도 필요합니다.

### 6-8. 파일명에 특수문자 금지
정산 시스템이 `\ / : * ? " < > |` 및 대괄호·괄호가 든 파일명을 거부합니다.
`_sanitize()` 는 **화이트리스트 방식**(한글·영문·숫자·공백·하이픈·밑줄만 허용)입니다.
중복 파일명도 `(2)` 가 아니라 `-2` 를 씁니다.

### 6-9. 비용이 비쌈
회의 1건에 **$0.7 ~ $1.0**, 20~35턴이 듭니다. PDF·이미지를 읽어서 토큰 소모가 큽니다.
사용자는 **Pro 플랜**이라 몇 건만 돌려도 한도에 걸립니다.
기능을 추가할 때 프롬프트를 불필요하게 늘리지 마세요.

---

## 7. 회의록 작성 규칙 (도메인 지식)

`core.py` 의 `RULES_DEFAULT` 에 있습니다. **사용자가 실제 회의록 7건을 보고 맞춘 것**이라
임의로 바꾸지 마세요. 아래는 특히 중요한 것들입니다.

### 회의내용(content)
- **정확히 2문단** (안건이 많으면 최대 3문단)
- 어미는 **`~하였음` / `~되었음` / `~하기로 하였음`** — `~함`, `~한다` 금지
- **회의 개최 사실·일시·장소를 쓰지 않음** (표에 이미 있으므로 중복)
  - 이건 한 번 되돌렸다가 다시 원복했습니다. 예시 1건만 보고 허용했는데, 나머지 5건이 모두
    개최 서술 없이 곧바로 안건 논의로 시작했습니다.
- 1문단: 안건 도입 → 배경·목적·검토 사항
- 2문단: `향후 ~에 대해서도 논의하였음` → 세부 방안·후속 조치 → `~하기로 하였음` 으로 마무리

### 절대 쓰지 않는 것 (회의내용 본문에서)
1. `회의비 집행을 위하여 회의를 개최함` 같은 지출 목적 서술
2. 예산 항목 (국고·직접비·그밖의사업운영경비·내부결재)
3. 식사 장소·메뉴·결제 수단·카드사·영수증
4. 지출결의·결재 라인·검토자·협조자·서명·문서번호
5. 참석 인원 수와 금액

> 표의 `회의비 사용처`·`금액` 칸에는 정상적으로 들어갑니다. **본문에서만** 뺍니다.

### 보완요청(supplement) — 4가지만
1. 서류 간 회의 일자·시간 불일치
2. 결제 시각이 회의 **시작**보다 이른 경우
3. 결제 시각이 회의 **종료**보다 **1시간 미만**으로 늦은 경우
4. 금액 **500,000원 이상**인데 참석 서명부가 없는 경우

> **결제 시각이 종료보다 1시간 이상 늦은 것은 정상입니다.** 회의 후 식사를 하기 때문입니다.
> 이걸 문제로 지적하는 버그가 있었습니다. 규칙에 정상/비정상 예시를 함께 넣어 고쳤습니다.

### 참석자 칸
회의비 기안공문의 **단가별 인원 수를 합산**(`attendeeCount`)해 그 수만큼 칸을 만듭니다.
예: `1인 15,000원 × 8명` + `1인 3,000원 × 2명` → **10칸**

서명부에서 확인된 이름부터 채우고 나머지는 **빈칸**으로 둡니다. 이름을 지어내지 않습니다.

### 서명부
**500,000원 이상일 때만 필수**입니다. 미만이면 없어도 보완사항으로 적지 않고,
회의계획서·공문 등 다른 서류에서 참석자를 찾습니다.

---

## 8. 사용자가 직접 바꿀 수 있는 것

| 파일 | 위치 | 내용 |
|---|---|---|
| `규칙.txt` | `.exe` 옆 | 회의록 문체·판단 기준. 저장하면 다음 작업부터 반영 |
| `설정.txt` | `.exe` 옆 | `이메일 = ...` (Claude 로그인 화면에 미리 채울 주소) |
| `오류기록.txt` | `.exe` 옆 | 오류 기록. 문제 보고 시 이 파일을 받으세요 |

`규칙.기본값.txt` 는 **사용자가 규칙을 고쳤는지 판단**하는 용도입니다.
`규칙.txt` 가 이 파일과 같으면 "손대지 않음"으로 보고 업데이트 시 최신 규칙으로 갱신하고,
다르면 사용자가 고친 것으로 보고 그대로 둡니다.

> **주의:** 사용자가 `규칙.txt` 를 고친 적이 있다면 `RULES_DEFAULT` 변경이 반영되지 않습니다.
> 규칙을 바꿨는데 사용자 쪽에서 안 먹으면 이걸 먼저 의심하세요.

---

## 9. 미해결 사항

### 9-1. Claude CLI 로그인이 반복해서 풀림
사용자가 데스크톱 Claude 앱을 함께 쓰는데, 그 사이 CLI 로그인이 무효화되는 것으로 보입니다.
**원인 미확인.** 데스크톱 앱과 CLI 가 자격 증명을 공유하는 부분의 간섭으로 추정합니다.

대응은 해두었습니다:
- 로그인 확인이 틀려도 **작업을 막지 않음** (예/아니오로 진행 선택)
- **`Claude 로그인` 단추**로 몇 초 만에 복구 (PATH 불필요)
- 이메일 자동 입력

빈도가 잦아지면 근본 원인을 파야 합니다.

### 9-2. 저장소가 공개 상태
소스에 사업단명·대학명이 있습니다. 사용자 이메일은 **일부러 소스에 넣지 않고**
`설정.txt`(사용자 PC)에만 저장하도록 했습니다. 비공개 전환은 사용자가 판단할 사항입니다.

### 9-3. 비용
회의 1건당 약 1,000원. Pro 플랜에서 한 번에 2~3건씩 나눠 돌리도록 안내했습니다.
프롬프트를 줄이거나 자료를 미리 추출해 넘기면 개선 여지가 있습니다.

### 9-4. 사양서와 어긋난 부분
원본 사양서(`회의록 자동 작성 도구 제작요청서`)와 다르게 구현한 곳이 있습니다.
전부 실제 버그를 고치는 과정에서 의도적으로 바꾼 것입니다. 대표적으로:
- `find_claude()` 하드코딩 경로 → `shutil.which()` 기반
- `.docx` 3개 파트만 → `styles.xml` 등 추가 (한글 호환)
- 출력 형식 `.docx` → `.hwpx`
- 출력 폴더 선택 → 회의 폴더에 저장

**사양서를 다른 사람에게 다시 넘겨 새로 만들게 하면 고쳤던 버그가 그대로 재현됩니다.**

---

## 10. 작업 시 권장 흐름

1. `core.py` 수정
2. 로컬에서 검증 — 이 컨테이너에는 tkinter 가 없으므로 스텁으로 대체해 import
   ```python
   tk = types.ModuleType("tkinter"); tk.Button = FakeBtn ...
   sys.modules.update({"tkinter": tk, "tkinter.ttk": ttk, ...})
   ```
3. 실제 회의 폴더를 흉내 낸 텍스트 파일로 `process_meeting()` 을 끝까지 실행
   (이 환경에도 `claude` CLI 가 있어 실제 동작 확인 가능)
4. `.hwpx` 를 만들었다면 **격자 검증** 필수
5. 커밋·푸시 → Actions 빌드 대기 → 릴리스에서 받아 재확인
6. 사용자에게는 **"그냥 켜시면 됩니다"** 라고 안내 (다운로드 불필요)

### 검증에 쓸 수 있는 것
- `python3 -m py_compile core.py` — 문법
- 이 컨테이너의 `claude` CLI — 실제 회의록 생성 (한 번에 $0.3~1.0 소요, 남용 금지)
- `zipfile` + `xml.dom.minidom` — `.hwpx` 구조 검증
- `pip install python-docx` — `.docx` 개봉 확인

---

## 11. 사용자 응대 시 참고

- 오류가 나면 **`오류기록.txt`** 를 요청하세요. 원인이 대부분 그 안에 있습니다.
- 화면 스크린샷도 유용합니다. 실제로 여러 번 원인 파악의 결정적 단서였습니다.
- 사용자는 개발자가 아닙니다. 명령어를 드릴 때는 **한 줄로 복사·붙여넣기 가능한 형태**로,
  PowerShell 연속 입력(`>>`)에 걸리지 않게 주의하세요.
- `claude` 는 PATH 에 없으므로 항상 전체 경로를 안내하세요:
  `"%USERPROFILE%\.local\bin\claude.exe"`
